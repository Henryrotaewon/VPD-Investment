name: MAGI2 Paper Execution (Railway Authority)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  railway-authority:
    runs-on: ubuntu-latest
    steps:
      - name: MAGI2 execution moved to Railway
        run: |
          echo "MAGI2 PAPER execution is disabled in GitHub Actions."
          echo "Railway is the single authoritative MAGI2 runtime and persistent-state owner."
          echo "Use the Railway service for monitor/morning/refill/report operations."
